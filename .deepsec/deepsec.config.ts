import { defineConfig } from "deepsec/config";

export default defineConfig({
  projects: [
    { id: "librebook_flutter", root: "C:/Users/user/librebook_flutter" },
    // <deepsec:projects-insert-above>
  ],
});
